export function windowsVersionFor(version: string): string

export function validateDesktopReleaseVersion(
  version: string,
  shortVersion: string,
  shortVersionWindows: string,
): string

export function validateReleaseTag(tag: string, version: string): string
